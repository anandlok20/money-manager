{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "BOSS_DIR=\"/Users/anandlok/Claude Apps/money-manager/.claude-boss\" && INPUT=$(cat) && SESSION_ID=$(echo \"$INPUT\" | jq -r '.session_id // \"unknown\"') && TIMESTAMP=$(date -u +\"%Y-%m-%dT%H:%M:%SZ\") && echo \"{\\\"timestamp\\\":\\\"$TIMESTAMP\\\",\\\"session_id\\\":\\\"$SESSION_ID\\\",\\\"project\\\":\\\"money-manager\\\"}\" >> \"$BOSS_DIR/tokens.jsonl\" && echo \"[$TIMESTAMP] Session: $SESSION_ID | Project: money-manager\" >> \"$BOSS_DIR/analysis.log\" 2>/dev/null || true",
            "statusMessage": "Saving session to Claude Boss..."
          }
        ]
      }
    ],
    "PostCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "BOSS_DIR=\"/Users/anandlok/Claude Apps/money-manager/.claude-boss\" && INPUT=$(cat) && SUMMARY=$(echo \"$INPUT\" | jq -r '.summary // \"(no summary)\"') && SESSION_ID=$(echo \"$INPUT\" | jq -r '.session_id // \"unknown\"') && TIMESTAMP=$(date -u +\"%Y-%m-%dT%H:%M:%SZ\") && echo \"[COMPACT $TIMESTAMP] Session: $SESSION_ID | $SUMMARY\" >> \"$BOSS_DIR/analysis.log\" 2>/dev/null || true",
            "statusMessage": "Saving compact summary to Claude Boss..."
          }
        ]
      }
    ]
  }
}
